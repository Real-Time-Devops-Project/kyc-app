name: Build and Push vKYC Service

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t kyc-vkyc-service:latest .

      - name: Push to Registry (Stub)
        run: echo "Pushing kyc-vkyc-service:latest to registry..."
